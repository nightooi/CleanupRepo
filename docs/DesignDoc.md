# Sad Attempt at a DesignDoc

## Components

* List displaying children
  * Children can't and should'nt be defined by the list
* Card Component That displays a info
  * the component should make use of var() css
        when it comes to w&h mostly so i can reuse it

